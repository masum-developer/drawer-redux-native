import { ADD_ITEM, REMOVE_ITEM } from "../ActionTypes";

export const Reducers = (state = [], action) => {
    switch (action.type) {
        case ADD_ITEM: {

        }
        case REMOVE_ITEM: {

        }
    }
}